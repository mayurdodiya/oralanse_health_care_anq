const moment = require("moment");
module.exports = {
    // donorResponse: (response) => {
    //     const obj = {
    //         full_name: response.full_name,
    //         blood_group: response.blood_banks.blood_types.name,
    //         age: response.age,
    //         gender: response.gender,
    //         phone_no: response.phone_no,
    //         registration_date: response.registration_date,
    //         address: response.address,
    //         blood_components: response.blood_components ? response.blood_components : null,
    //     }

    //     return obj
    // },

}